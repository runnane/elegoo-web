node_modules
dist
.cache
.env
*.log
.git
.github
data/CentauriCarbon2
data/raw
data/gcode-cache
data/logs
data/reports
data/state.json
data/moonraker-db.json

# Agent worktrees live under .claude/worktrees/ as full checkouts, each with its own
# node_modules. The docker context does not honour VCS ignore rules, so without this every
# one of them is sent as build context and COPY . . carries it into the build stage (ELEG-88).
.claude
