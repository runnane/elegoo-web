{
  "mcpServers": {
    "respawn-control": {
      "type": "http",
      "url": "https://respawn.srv.jont.no/mcp",
      "headers": {
        "Authorization": "Bearer ${RESPAWN_MCP_TOKEN}"
      }
    }

  }
}
